const debug = require('debug')('app:user.update.controller');

module.exports = async (req, res) => {
  try {
    // const pageModel = await builder();
    // debug(pageModel);
    // debug(pageModel.content);
    // res.render('article.books.pug', pageModel);
  } catch (e) {
    debug(e);
  }
};
