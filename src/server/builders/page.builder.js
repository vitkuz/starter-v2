const {  readMenuArr,
  createMenuArr,
  updateMenuArr,
  listsMenuArr } = require('../_sandbox/sandbox.generate.menu');
const categoriesMenu = require('../data/categoriesMenu');



const page = {
  readMenuArr,
  createMenuArr,
  updateMenuArr,
  listsMenuArr,
  categoriesMenu
};

module.exports = page;
